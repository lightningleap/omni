/**
 * Category background — the decorative floating line-art doodles have been
 * removed for a clean, distraction-free, product-first canvas.
 *
 * Kept as a no-op so existing imports keep working; every page now sits on the
 * shared warm off-white + grain canvas (globals.css + BrandBackground).
 */
export default function CategoryDoodleBackground(_props: { category?: string }) {
  return null;
}
